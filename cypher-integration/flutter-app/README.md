# 第一版 Flutter 集成副本：仅供参考

<!-- harbeat:reference-only -->
`lib/data/` 中包含历史数据模型、鉴权、歌曲和 RK 同步服务；`android/` 为历史构建相关文件。这不是已确认的新 APK 源码，也不是保证可以独立构建的完整新项目。

第二版手机操作后续重构，预计不直接使用本目录。旧接口字段、设备地址和音频控制逻辑不约束新 APK 或新后端。本轮不构建、不部署、不与新前端合并。

见 [参考代码说明](../../docs/repository/reference-code.md)。
