# Analysis fixtures

这里保存不含版权音频和本地路径的算法合同 JSON：ready、degraded、unavailable、invalid、timeout、cancel、旧版兼容。

当前同风格接歌 ready fixtures：

- `same-style-track-preprocess-v1.ready.json`
- `same-style-pair-score-v1.ready.json`

它们只使用虚构 ID、hash 和 `storage_key`，可直接交给混音协作者开发读取层，不代表 Jetson 已经生成了对应真实资产。
