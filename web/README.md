# 第一版网页代码：仅供参考，后续重构

<!-- harbeat:reference-only -->
此目录是旧 React/Vite 网页和控制逻辑，预计不直接用于第二版。是否需要网页端、是否复用某些界面，须重新确认；不作为新 APK 的实现依据。

`src/` 保存页面与客户端逻辑，`package.json` 保存历史构建依赖。旧请求地址、字段和播放操作不约束新后端；本轮未构建、部署或修改网页逻辑。

完整边界见 [参考代码说明](../docs/repository/reference-code.md)。
