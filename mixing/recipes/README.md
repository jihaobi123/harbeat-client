# 演示配方

`render_demo_v3.py`、`render_hiphop_v3.py` 与 `build_hiphop_v3_review.py` 已整理为可指定输入目录的入口，见上级 README。

其他脚本保留 V1/V2/早期 V3 交付过程，仍需对应的历史 `outputs/` 快照，不能作为没有数据的新 checkout 的默认命令。它们有审计价值，但不会在 CI 中自动渲染私人曲库。报告样式和用户提供的 logo 已单独归档，Hip-Hop 报告不再依赖旧试听页面。
