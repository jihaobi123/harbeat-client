# 保护模式验证 · 2026-09-23

## 完成的检查

- TypeScript 与独立保护页构建通过；原实时页也可构建，旧发布文件未覆盖。
- `GUARD_CATALOG=... npm --prefix web test -- src --no-cache`：137 项通过，3 项现有可选数据测试跳过。
- 六首真实曲目：25 个退出候选，41 个进入窗口，197 个速度适配伴奏素材；远端 197 个文件 SHA 校验通过。
- 30 个有方向的曲对在没有人工确认时全部被阻止；测试进程内使用明确标记的合成确认记录，29 个曲对存在几何可行组合。这不是 29 个曲对的听感通过率；合成确认未写入目录、网页或用户浏览器。
- 未确认、源文件/正文/进入素材指纹改变、正文起句否决、缺少人声区间、跨段落、拍网格不匹配、素材未就绪均可阻止规划。
- 注入规划器被拒绝时保持 A deck，不调用普通规划器；通过合成确认的计划只在退出前启动伴奏，原曲正文在 A 增益归零时接管，保留确认证据和保护策略版本。
- 真实公网浏览器：A 段尾预览完整播放后才解锁确认按钮，未自动提交确认；无确认请求被拒绝，A 继续播放；无页面异常，390px 手机宽度无横向溢出。截图与浏览器会话记录保存在本机 `outputs/v3-protected-20260923/`。
- 初始发布目录 `reviews` 为空。没有自动伪造人声乐句或段落的人工真值。

## 测试范围与未验证项

- 没有主观试听验收；技术播放成功不等于段落、歌词或音质已经通过。
- 真实歌曲的通过路径依赖用户试听确认。本轮未代替用户确认任何音乐边界。
- 歌词语义识别、整曲精确小节校准、自适应人声尾音识别未实现；已有长 VAD 区间仍保留。
- 沿用最多 150 秒演示素材，不能代表完整曲库覆盖。
- 曾运行不限定目录的全部测试：未跟踪的旧 `web/diagnostics/multi-probe.test.ts` 因缺少外部路径变量失败。未修改或提交这份旧诊断。正式源码测试通过如上。
- 浏览器音频线程事件是渲染块时间，不是扬声器或蓝牙实测延迟。
