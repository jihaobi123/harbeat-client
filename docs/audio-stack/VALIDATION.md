# 验证与复现

本次在独立 checkout 中安装依赖，使用合成音频检查 DSP，不从 NAS 获取数据的普通检查覆盖：

- 分析、来源绑定、接口、DJ 计划和 V3：110 项。
- 曲库权限、会话、转场规划、分轨桥接：48 项。
- 完整预处理 Schema、发布和批量入库：8 项（在独立源码根运行）。
- 页面单元测试：80 项。

验证命令和依赖以 [GitHub Actions](../../.github/workflows/audio-stack.yml) 为准。完整模型分析需要 Jetson 的模型环境；实际声卡播放、所有 GPU 路线和人工边界准确性不属于这些测试覆盖。

## V3 声音回归

使用新目录的 `mixing/recipes/render_demo_v3.py`、显式传入原曲清单和原同事参照，重新渲染六首 Future Bass。

- 18,971,667 个 44.1 kHz 立体声采样帧，430.196531 秒。
- 解码样本完全相同，最大样本差为 0。
- MP3 文件 SHA256：`d87180977de7494bf3e2e4e2640049bd50c712fb4d25c8fffe6861eb93182a41`，与原确认版相同。

该结论针对本机这次 FFmpeg 环境；跨编码器环境不能预先保证 MP3 字节相同。真实音频保存在 NAS/本地交付目录，不进入仓库。

## 来源检查

```bash
python scripts/verify_audio_sources.py
```

此命令验证从 Jetson 获取的 381 个源码文件与冻结 V3 原算法/计划。今后修改预处理时，需显式更新发布及来源记录，不能把改变后的内容继续标作原部署快照。

前端生产构建已通过。Hip-Hop 配方重新生成的顺序、五个转场和全部逐曲渲染参数与已发布版一致。

本次 GitHub 分支的 CI 结果需在推送后查看；本地检查通过不等于云端作业已经通过。源码整理不会自动触发服务器部署。
