# 音乐风格模型首轮落地验证：最终判断

> 生成时间：2026-08-28T19:24:59.356141+00:00

## 直接结论

- 当前实验能验证 **13 类**，不能验证 21 类；数据中缺少 8 类：drill, memphis_trap, moombahton, rage, rnb, soul_neo_soul, trap_soul, uk_garage。
- 13 类模型指标门槛：未通过；数据纯度筛查：未通过。
- 只看当前 13 类识别信号：最佳融合 Top-1 0.800、Macro-F1 0.796；完整模型门槛未过的模型项是融合增益仅 0.015。
- 运行时接入：**暂不允许**。未人工复核的模型辅助纯度标签不能当作最终真值。

## 三条路线的证据

- 嵌入 `embedding_logreg`：Macro-F1 0.781，Balanced Accuracy 0.785，Top-3 0.954。
- 技术特征 `technical_logreg`：Macro-F1 0.432，Balanced Accuracy 0.446，Top-3 0.785。
- 融合 `fusion_svm`：Macro-F1 0.796，Balanced Accuracy 0.800，Top-3 0.954；相对嵌入增益 0.015。
- 最强单项技术特征 `track_log2_bpm`：Top-1 0.246，Macro-F1 0.221；完整 65 项见 `technical_feature_accuracy.csv`。

## 数据不干净风险

- 模型辅助纯度等级：{"A": 49, "B": 2, "C": 14}。
- 检出可能切换风格的歌曲：21/65。这些点位是人工试听队列，不是已确认的第二标签。
- 每类至少三首 A/B：否。
- MusicBrainz 身份审计：65 首；状态 {"miss": 20, "identity_confirmed": 35, "identity_and_tags": 8, "error": 2}；有公开 tag 8 首。Miss/无 tag 不自动视为标签冲突。
- 窗口子组：节拍对齐 0.840，固定回退 0.667；这是难度相关性，不是因果结论。

## 下一步决策

1. 暂停接入；优先清洗 C 级与冲突片段，再扩充每类歌曲，不应在这 65 首上增加模型复杂度。
2. 为缺失 8 类各收集至少 15～30 首、跨艺人且版本明确的歌曲，再重新进行同样的艺人隔离验证。
3. 只有 21 类都存在独立样本并通过逐类 Recall/混淆检查，才能回答 21 类正式模型是否成立。
4. 当前最佳路线是在同一组四折结果中选出的，存在探索性选优偏差；扩充后必须保留完全未参与选择的外部测试集。
