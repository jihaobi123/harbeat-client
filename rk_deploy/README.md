# 第一版部署与接口副本：仅供参考，后续重构

<!-- harbeat:reference-only -->
此目录包含历史 `deploy.sh`、`live_api.py`、`live_models.py`。文件名或目录名不代表它们与当前 RK/Jetson 上的代码一致。

第二版预计不直接使用这些副本；真实混音、播放、下载和控制由 RK 负责人重构。不要直接执行旧部署脚本或复制这些文件覆盖设备服务。

本轮只补说明，不修改脚本、设备、接口或数据库。边界见 [参考代码说明](../docs/repository/reference-code.md)。
